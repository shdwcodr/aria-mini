# ARIA — pinned dependencies
#
# These versions are the recommended minimum-known-good set for this codebase
# (scikit-learn's MLPClassifier API, shap.Explainer / shap.sample, and
# lime.lime_tabular.LimeTabularExplainer used throughout). They are NOT a
# `pip freeze` capture of the original development environment — that
# environment is not available to us. Before treating a run as a faithful
# reproduction, regenerate this file from your own working environment with:
#
#   pip freeze > requirements.lock.txt
#
# and archive requirements.lock.txt alongside any results you report.
#
# Python: developed and tested on Python 3.11–3.12. Not tested on 3.13+.

numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
joblib==1.4.2
shap==0.45.1
lime==0.2.0.1
matplotlib==3.8.4
scipy==1.13.0

# Optional — only needed if you use the Streamlit/Plotly demo app
# (not required to reproduce any table or figure in the paper)
streamlit==1.34.0
plotly==5.22.0
